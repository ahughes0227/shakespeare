id: resolve
version: "1.0.0"
standing_goal: Determine each item's field values and render its name.
catalog: [name.render, text.normalize, batch.window]
config_groups: [naming, confidence, schedule]
produces: [ResolvedNames]
# How many times it may reorganise before giving up. This is where a capability
# decides it has done part of the work and should carry on.
max_rounds: 40
prompt_version: "1.0.0"
