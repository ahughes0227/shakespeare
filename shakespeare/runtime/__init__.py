"""The system that executes.

It owns everything a run does rather than decides: the goal loop and its gates, component
execution and verification, staging and commit, and the evidence — artifacts, the audit
log and telemetry — that a run leaves behind.
"""
