"""The content_extract family.

Derive text and metadata from immutable input bytes.

Its contract, risks and failure modes are declared once in `family-context.yml` and
inherited by every operator here.
"""
